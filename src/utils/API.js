// in dev mode
const URL_PREFIX = "http://localhost:3000"
// in prod mode
// const URL_PREFIX= "https://codescaper.herokuapp.com"

const API = {
    login: () => {
        return;
    },
    signup: () => {
        return;
    },
    getPoll: () => {
        return;
        
    }
}

export default API